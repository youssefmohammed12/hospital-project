# HealthBridge Final Fixes - Remaining Issues

## Issue 1: Feature Upcoming "Next Appointment" Component
- The `#featured-upcoming-container` is rendered by inline script in `pages/patient/appointments.html`
- It uses CSS class `.featured-upcoming-card` which ONLY has responsive overrides in appointments.css — NO base styling exists
- This is why it renders as raw HTML - the base card, icon, body, label, title, meta, and actions styles are completely missing
- **Fix**: Add proper CSS base styles for `.featured-upcoming-card` in appointments.css

## Issue 2: Missing "Confirmed" Filter
- Need to add a `<button>` for "Confirmed" filter in filter-chips
- API needs to handle `confirmed` status
- JS needs to handle the filter

## Fixes Applied:
- [x] Add CSS for featured-upcoming-card base styles
- [x] Add "Confirmed" filter chip to appointments.html  
- [x] Update appointments API to handle 'confirmed' filter
- [x] Update AppointmentsPage JS to handle confirmed in status labels/colors
- [x] Remove deprecated optimistic UI code in booking-wizard.js
- [x] Fix SQL cartesian product in PatientPortalService.php
- [x] Fix VisitWorkflowService auto-completion
- [x] Fix PatientPortalService status categorization